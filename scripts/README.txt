====================================================
  OffSec — Execution Scripts
====================================================

WINDOWS USERS:
  Step 1: Double-click  setup.bat          (run once)
  Step 2: Double-click  start_offsec.bat   (run every time)

LINUX / macOS USERS:
  Step 1: chmod +x setup.sh start_offsec.sh
          ./setup.sh                        (run once)
  Step 2: ./start_offsec.sh                (run every time)

====================================================
